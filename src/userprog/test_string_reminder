make; cd build; pintos --filesys-size=2 -p ../../examples/echo -a echo -- -f -q run 'echo one two three'; cd ..

To test before implementing process_wait()
make; cd build; pintos --filesys-size=2 -p ../../examples/echo -a echo -- -f run 'echo one two three'; cd ..

Permanent filesystem:
IN BUILD DIRECTORY:
pintos-mkdisk filesys.dsk --filesys-size=2
OUTSIDE OF BUILD DIRECTORY:
make; cd build; pintos -p ../../examples/echo -a echo -- -f -q run 'echo one two three'; cd ..